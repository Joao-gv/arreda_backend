services:
  db:
    image: postgres:15
    container_name: arreda-db
    restart: always
    environment:
      POSTGRES_USER: arreda_user
      POSTGRES_PASSWORD: ifmg_arreda_app
      POSTGRES_DB: arredadb
    ports:
      - "5432:5432"